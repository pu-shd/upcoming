# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/

# macOS
.DS_Store

# Local env / secrets
.env
.env.local

# Build / generated output
build/
dist/
out/

# Agent tooling (never commit)
CLAUDE.md
.claude/

# Generated feeds -- published to the `published` branch and Pages, never to main
/feeds/
/combos/
/build/
# Fetched ICS captures. `make publish FETCH=1` writes here; the committed fixtures under
# tests/fixtures/feeds are the ones under version control.
/.feeds/
