"""Multi-source campus event feeds.

One ``events.json`` per data source, plus combined feeds composed from them by declared set
algebra. See ``config/sources.yaml`` for the registry and ``docs/`` for the runbook.
"""

__version__ = "0.1.0"
